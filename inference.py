def main():
    print("[START] task=openenv", flush=True)

    reward = 0.5
    print(f"[STEP] step=1 reward={reward}", flush=True)

    print("[END] task=openenv score=0.5 steps=1", flush=True)


if __name__ == "__main__":
    main()