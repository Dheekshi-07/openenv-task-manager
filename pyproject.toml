[project]
name = "openenv-task-manager"
version = "0.1.0"
description = "OpenEnv task manager"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"